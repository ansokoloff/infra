---
 - name:  Install mc 
   ansible.builtin.package:
    name: mc
    state: present
    
 - name:  Install net-tools 
   ansible.builtin.package:
    name: net-tools
    state: present

#  - name:  Copy config 
#    ansible.builtin.copy:
#     src: roles/vsftpd/files/vsftpd.conf 
#     dest: /etc/vsftpd.conf

#  - name:  Run vsfrpd 
#    ansible.builtin.service:
#     name: vsftpd 
#     state: started
